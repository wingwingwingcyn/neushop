server:
  port: 8082
spring:
  application:
    name: product
  profiles:
    active: datasource,regist,mybatis
  cloud:
    nacos:
      config:
        server-addr: 192.168.80.128:8848
        prefix: cyn
        file-extension: yml
