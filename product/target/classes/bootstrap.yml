server:
  port: 8082
spring:
  application:
    name: product
  profiles:
    active: datasource,regist,mybatis
  cloud:
    nacos:
      config:
        server-addr: 192.168.182.10:8848
        prefix: mz
        file-extension: yml
