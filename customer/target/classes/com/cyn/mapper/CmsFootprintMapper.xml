<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyn.mapper.CmsFootprintMapper">

    <delete id="delAll" parameterType="java.lang.Long">
       DELETE FROM cms_footprint WHERE customer_id = ${customerId} and is_fc=0
    </delete>

    <delete id="delAllCollection" parameterType="java.lang.Long">
       DELETE FROM cms_footprint WHERE customer_id = ${customerId} and is_fc=1
    </delete>

</mapper>
