server:
  port: 8083
spring:
  application:
    name: customer
  profiles:
    active: datasource,regist,minio,mybatis
  cloud:
    nacos:
      config:
        server-addr: 192.168.80.128:8848
        prefix: cyn
        file-extension: yml