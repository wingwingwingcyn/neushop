server:
  port: 8081
spring:
  application:
    name: admin
  profiles:
    active: datasource,regist,minio,mybatis
  cloud:
    nacos:
      config:
        server-addr: 192.168.217.139:8849
        prefix: neu
        file-extension: yml