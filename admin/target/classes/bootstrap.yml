server:
  port: 8081
spring:
  application:
    name: admin
  profiles:
    active: datasource,regist,minio,mybatis
  cloud:
    nacos:
      config:
        server-addr: 192.168.80.151:8848
        prefix: zym
        file-extension: yml