server:
  port: 8081
spring:
  application:
    name: admin
  profiles:
    active: datasource,regist,minio,mybatis
  cloud:
    nacos:
      config:
        server-addr: 192.168.182.10:8848
        prefix: mz
        file-extension: yml